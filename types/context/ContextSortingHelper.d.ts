export default class ContextSortingHelper {
    private static readonly MAX_INDEX;
    private static readonly MULTIPLICATOR;
    private static CONTEXTS_ORDER;
    static _initiliaze(): void;
    static getIndex(context: string): number | null;
}
